# Журнал инструментов — ДЗ №2

Только дополняется, старые записи не редактируются.

## 2026-08-29 · Сессия 1 · проектный скилл `monitor-competitor-tariffs`
- **Тип:** скилл ассистента (Claude Code, `.claude/skills/`)
- **Установка:** создан вручную, файл
  `.claude/skills/monitor-competitor-tariffs/SKILL.md` (frontmatter `name` +
  `description`, тело — процедура)
- **Зачем:** обязательный пункт 3 ДЗ №2 — повторяемый workflow «мониторинг
  тарифов конкурентов», применимый к реальной задаче Mirocard
- **Проверка:** применён сразу же вручную (не через автоматический вызов
  скилла, так как скиллы проекта подхватываются при старте новой сессии, а
  не в середине текущей) — по описанным в нём шагам собраны факты по 6
  конкурентам + Pipl в этой же сессии, результат — `hw-02/facts.md`

## 2026-08-29 · Сессия 1 · Google Chrome (headless) как конвертер HTML→PDF
- **Тип:** CLI (существующее приложение, новое использование)
- **Установка:** не устанавливался — уже стоял в системе
  (`C:\Program Files\Google\Chrome\Application\chrome.exe`); использован
  флаг `--headless --disable-gpu --print-to-pdf`
- **Зачем:** собрать `mirocard-vs-konkurenty-2.pdf` из `pdf-src/brief.html`
  без установки дополнительных пакетов (`wkhtmltopdf`, `weasyprint`,
  `pandoc` — ни один не найден в системе, см. тупик ниже)
- **Проверка:** результат (6 страниц) открыт и провизуален в браузере
  (`mcp__Claude_Browser`) постранично — таблицы не обрезаны, футер не
  перекрывает текст

## 2026-08-29 · Сессия 1 · `gh` CLI — тупиковая попытка
- **Тип:** CLI
- **Установка:** не установлен. Проверил `gh --version` в git-bash и в
  PowerShell — команда не найдена ни там, ни там
- **Зачем:** нужен был для создания приватного/публичного репозитория на
  GitHub и последующего push от имени пользователя без ручных действий
- **Почему не пошёл дальше:** установка `gh` потребовала бы ещё и
  `gh auth login` — интерактивный вход в чужой аккаунт, который агенту
  делать не следует. Решил не ставить и не логинить самостоятельно, а
  попросить пользователя создать репозиторий на github.com руками и
  передать ссылку — push сделаю сам через обычный `git push` уже с готовым
  remote, это не требует от меня ввода учётных данных
- **Проверка:** н/п (инструмент не установлен, решение — обойтись без него)

## 2026-08-29 · Сессия 1 · Python / pandoc / wkhtmltopdf / weasyprint / node — тупиковая проверка
- **Тип:** CLI / библиотека
- **Установка:** не устанавливались. Проверено наличие в системе — ни один
  не найден (`python` в PATH ведёт на заглушку Microsoft Store, `node`,
  `pandoc`, `wkhtmltopdf`, `weasyprint` отсутствуют)
- **Зачем:** рассматривались как альтернативный способ сборки PDF
- **Почему не пошёл дальше:** headless Chrome уже был в системе и закрыл
  задачу без установки чего-либо нового — решил не тащить лишние
  зависимости ради одной конвертации
