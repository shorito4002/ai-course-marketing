# Сессия 1 — Продолжение проекта: обновление сравнения + Pipl + workflow

- **Дата:** 2026-08-29
- **Ассистент:** Claude Code (Sonnet 5)
- **Режим разрешений:** Default — подтверждение каждого действия. Выбрано
  осознанно: дедлайн сегодня же (18:00), плюс работа с git/GitHub, где цена
  ошибки (лишний push, снос истории) выше цены лишнего клика подтверждения.
- **Статус:** в работе

## Промпты

Дословно, по порядку.

> смотри, сейчас будем делать следующее задание по вайбкодингу, вот смотри
> ошибки с прошлого задания, чтобы не допустить их в этом

(к сообщению приложено изображение — карточка оценки за ДЗ №1: 72/100,
«Никита Морозов», тег `SHORITO4002/AI-COURSE-MARKETING`, комментарий
преподавателя «Всего одна сессия, нет инструментов. Это немножко занизило
балл, а так окей», разбивка по критериям и списки «что получилось хорошо» /
«над чем поработать».)

> @"C:\Users\nisk4\OneDrive\Документы\ДЗ-2/"
> Формально в задании это нигде явно не прописано — но по логике курса и по
> тому, что видно из твоей оценки за ДЗ №1, разумнее продолжать в том же
> репозитории, а не начинать с нуля.
> Почему:
>
> * Задание прямо поощряет брать свой реальный проект, а не выдумывать новую
>   тему — ты и так продолжаешь ту же тему сравнения конкурентов
> * Один из плюсов, который ценят в курсе — история изменений в git видна
>   целиком: чем длиннее и последовательнее история коммитов, тем лучше
>   видно процесс работы (а «один коммит за раз» как раз было замечанием в
>   прошлый раз)
> * Начинать новый репозиторий значит терять эту историю, а она как раз
>   работает в плюс
>
> Как сделать технически:
>
> ```
> В этом репозитории (тот же, что для ДЗ №1) создай новую папку hw-02
> для нового задания. Файлы из ДЗ №1 не трогай и не перезаписывай —
> они остаются как есть, как история. В hw-02 положи все новые файлы:
> spec.md, таблицу, workflow, README для этого задания, PDF.
> ```
>
> В README к новой папке стоит добавить одну строчку — что это продолжение
> проекта из ДЗ №1, ссылку на старую папку/таблицу, чтобы преподаватель сразу
> видел связь.

## Размышления

**Что не было ясно и как разрешалось:**

- Первое сообщение (карточка оценки) относится не к пользователю по email
  (`burovcevaveronika1@gmail.com`), а показывает имя «Никита Морозов» — но
  git `user.name`, уже настроенный на машине глобально (`shorito4002`),
  дословно совпадает с тегом репозитория на карточке
  (`shorito4002/ai-course-marketing`). Несовпадение отображаемого имени с
  git-логином — не противоречие: воспринял это как одну и ту же учебную
  работу и продолжил её.
- Перед тем как трогать файлы, проверил `git status` в `TestTask` — **git не
  был инициализирован вообще**, хотя отчёт по ДЗ №1 прямо упоминает «один
  коммит». То есть либо коммит существовал где-то ещё и не попал в эту
  папку, либо предыдущая сессия описала git-действия, которых по факту не
  было. Зафиксировал это как факт, а не подогнал историю задним числом —
  `git init` и первый коммит сделаны сейчас, честно с сегодняшней датой.
- `materials/` (PDF/md занятия) в git не кладу — это раздаточные материалы
  курса, а не продукт работы; исключил через `.gitignore`, чтобы не раздувать
  репозиторий. Решение не было продиктовано пользователем напрямую, это моя
  интерпретация «файлы ДЗ №1 не трогать» — не трогать значит не удалять и не
  редактировать, но не обязывает добавлять в git всё подряд.
- 7-й конкурент не был назван пользователем — по правилам `AGENTS.md`
  агент не может сам расширять список источников без вопроса. Нашёл через
  веб-поиск три кандидата (Card.Club, Exnode, Pipl), проверил каждый на
  «открывается без регистрации» — у Card.Club и Exnode `WebFetch` не
  вернул тарифы (503 у Exnode, пустая страница у Card.Club), у Pipl
  (`pipl.io/bank-cards/`) тарифы открылись сразу и по всем 5 критериям
  нашлись цифры. Предложил Pipl пользователю явно, получил подтверждение,
  только после этого добавил в план.
- Перед созданием репозитория на GitHub проверил `gh` CLI — не установлен
  ни в git-bash, ни в PowerShell. Решил не ставить и не логинить его
  самостоятельно (аутентификация — не моё действие), а попросить
  пользователя создать пустой репозиторий на github.com руками и передать
  ссылку; push сделаю сам после этого с подтверждением.
- Прогнал `grep` на секреты (`api[_-]?key|token|password|secret|sk-...`)
  по всему `TestTask` перед первым коммитом. Нашлось одно совпадение — в
  `materials/reference.md`, но это документация Claude Code про сами
  переменные `ANTHROPIC_API_KEY`/`GITHUB_TOKEN` (примеры кода), не реальный
  ключ. Файл всё равно не попал в git (см. выше про `materials/`).

## Использованные инструменты

| Инструмент | Действие | Зачем |
|---|---|---|
| Bash / git | `git init`, коммит ДЗ №1 как есть | Начать историю, зафиксировать состояние «до» |
| WebSearch | Поиск 7-го конкурента | Найти реальный сервис виртуальных карт, не входящий в список ДЗ №1 |
| WebFetch | Проверка тарифных страниц Exnode / Card.Club / Pipl | Отсеять источники, которые не отдают данные без входа |
| Grep | Поиск секретов по всему репозиторию | Требование чек-листа ДЗ №2 перед сдачей |
| AskUserQuestion | Уточнение объёма задачи, workflow, репозитория, режима разрешений, кандидата на 7-го конкурента | Решения, которые по правилам курса принимает человек, а не агент |
| **Agent (8 параллельных субагентов, `general-purpose`, в одной сессии)** | Каждому — свой сервис (Mirocard + 6 старых конкурентов + Pipl), задача перепроверить/собрать 5 фактов с цитатами | Пункт 2 ДЗ №2 («агенты запустить и попробовать») — реальная задача, не демо |

## Что нашли субагенты (важно для «Размышлений»)

Не гладкий прогон — два субагента сами нашли расхождения с записями ДЗ №1,
не подгоняя их под старый ответ:

- **Wayment (субагент W).** Заявленная в ДЗ №1 поддержка Visa/Mastercard не
  подтвердилась: полнотекстовый поиск не нашёл эти слова ни на одной
  проверенной странице. Субагент честно написал: «возможно, это было
  получено через Telegram-бота, недоступный для read-only проверки» — не
  придумал объяснение, а отметил границу своих возможностей.
- **EPN Cards (субагент E).** Заметил, что «пополнение от 3%» из ДЗ №1 на
  самом деле верхняя граница ступенчатой сетки 3–6,7% по обороту — нашёл
  это только благодаря указанию «используй полноценный fetch, не только
  текстовый сниппет» (тот же баг, что аудит нашёл в ДЗ №1 на этом же сайте
  — интересно, что грабли одни и те же).
- **PST.NET (субагент PS).** Нашёл через WebSearch сторонние страницы с
  более полными данными о пополнении, но не стал вносить их как факт —
  сослался на правило AGENTS.md «источник вне списка — повод спросить, а
  не расширить самому». Это ровно то поведение, которое требовалось, без
  дополнительного напоминания в промпте субагента про это конкретное
  правило (только общая ссылка на AGENTS.md).

Ни одно расхождение не сглажено в пользу более красивой картины — обе
находки перенесены в `hw-02/facts.md` и `hw-02/brief.md` с пометкой
«было / стало» и рекомендацией проверить вручную, где это возможно.

## Изменения в проекте

- Создан: `.gitignore` (корень)
- Создан: `hw-02/SPEC.md`
- Создан: `hw-02/sessions/session-1.md` (этот файл)
- Создан: `hw-02/facts.md` — перепроверка 6 конкурентов + Pipl, 8 параллельных субагентов
- Создан: `hw-02/brief.md` — обновлённое сравнение, 8 участников
- Создан: `.claude/skills/monitor-competitor-tariffs/SKILL.md` — обязательный workflow
- Создан: `hw-02/workflow/monitor-competitor-tariffs.md` — резюме workflow для тех, кто не открывает `.claude/`
- Создан: `hw-02/pdf-src/brief.html` и `hw-02/mirocard-vs-konkurenty-2.pdf`
- Создан: `hw-02/README.md`, `hw-02/sessions/TOOLS.md`, `hw-02/sessions/STATE.md`, `hw-02/REPORT.md`
- Git: 7 коммитов по ходу работы (не один в конце) — см. `git log`

## Финальный вердикт

**Сделано:** все три обязательных пункта ДЗ №2 — рабочий результат
(обновлённое сравнение 8 сервисов + PDF), агенты параллельно (8 субагентов
в одной сессии на реальной задаче, не демо), workflow (проектный скилл,
применённый сразу же на практике, не только описанный). Найдены и честно
зафиксированы два реальных расхождения с данными ДЗ №1 — не сглажены.

**Не сделано осознанно (необязательная часть):** worktrees, изоляция,
проверка на инъекции, оптимизатор контекста, голосовой ввод, замер
последовательно/параллельно — все по причине дедлайна в день начала
работы, разбор — в `hw-02/REPORT.md`.

**Осталось открытым на конец сессии:** push на GitHub — жду от
пользователя ссылку на созданный им репозиторий (`gh` CLI недоступен в
окружении, создать репозиторий или залогиниться агент не может и не
должен). Как только ссылка будет — добавляю remote и делаю push с
подтверждением, это последний шаг перед сдачей.

**Что бы сделал иначе, если бы было больше времени:** провёл бы честный
замер последовательно/параллельно для 8 субагентов (материалы занятия
прямо предупреждают, что параллельность не всегда быстрее — интересно
было бы проверить на этом объёме), и попробовал бы worktrees для трёх
независимых кусков (например, отдельная ветка на добавление Pipl,
отдельная — на workflow, отдельная — на PDF) вместо последовательной
работы в одной ветке.
